<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= title ? title + ' — Conduit' : 'Conduit' %></title>
    <!-- Import Ionicon icons & Google Fonts our Bootstrap theme relies on -->
    <link href="//code.ionicframework.com/ionicons/2.0.1/css/ionicons.min.css" rel="stylesheet" type="text/css">
    <link href="//fonts.googleapis.com/css?family=Titillium+Web:700|Source+Serif+Pro:400,700|Merriweather+Sans:400,700|Source+Sans+Pro:400,300,600,700,300italic,400italic,600italic,700italic" rel="stylesheet" type="text/css">
    <!-- Import the custom Bootstrap 4 theme from our hosted CDN -->
    <link rel="stylesheet" href="//demo.productionready.io/main.css">
    
    <% if (ENV_DEVELOPMENT) { %>
      <!-- Include livereload script for automatic code reloading in development -->
      <script src="http://localhost:35729/livereload.js"></script>
    <% } %>

    <meta name="csrf-token" content="<%= csrfToken %>">

    <script type="text/javascript">
      (function() {
        var env = window.env = window.env || {};
        var updates = <%= ENV_JSON %>;
        Object.keys(updates).forEach(function (key) {
          env[key] = updates[key];
        });
      })();
    </script>

    <script src="/build/polyfills.js" defer data-turbolinks-eval="false"></script>
    <script src="/build/main.js" defer></script>

    <% if (typeof extraScripts !== 'undefined') extraScripts.forEach(function (script) { %>
      <script src="<%= script %>" defer></script>
    <% }); %>

    <meta name="turbolinks-cache-control" content="no-preview">
  </head>

  <body>

  <%- include('./navbar') %>
